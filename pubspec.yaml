name: tickflow_chat
description: Client-side chat SDK for the Tickflow app — AI-first support assistant with human fallback on the same thread.
version: 0.1.0
publish_to: none
homepage: https://github.com/kaixuanx3/tickflow-chat-sdk

environment:
  sdk: ^3.10.3
  flutter: ">=3.22.0"

dependencies:
  fetch_client: ^1.2.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.0 # matches the app's pin; used only by the riverpod barrel
  http: ^1.2.0
  meta: ^1.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
